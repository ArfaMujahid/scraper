// Package config defines the Config struct and fail-fast validation for the
// scraper.
//
// Responsibility: Config struct (seeds, workers, depth, rate, timeout, format,
// user-agent, respect-robots, mode, owner, ui-addr, data-dir, retention,
// sweep-interval) + Validate() that fails fast before any crawling begins.
package config

// TODO: define Config struct and Validate().
